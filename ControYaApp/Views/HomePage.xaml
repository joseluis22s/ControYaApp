<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ControYaApp.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModel="clr-namespace:ControYaApp.ViewModels"
    x:DataType="viewModel:HomeViewModel">

    <VerticalStackLayout>
        <Label Text="dddddddddddddd" />
        <Label Text="{Binding NombreUsuario, StringFormat='Usuario: {0}'}" VerticalOptions="Center">
            <Label.FormattedText>
                <FormattedString>
                    <Span FontSize="Medium" Text="Usuario: " />
                    <Span FontSize="Large" Text="{Binding NombreUsuario}" />
                </FormattedString>
            </Label.FormattedText>
        </Label>
        <Button Command="{Binding ObtenerPedidosCommand}" Text="Obtener ordenes" />
    </VerticalStackLayout>
    <ContentPage.Background>

        <Color>#8db3d6</Color>

    </ContentPage.Background>
</ContentPage>